SELECT
    id,
    email,
    hashed_password,
    last_name,
    first_name,
    middle_name,
    phone,
    count_vacation_days,
    profession_id,
    role_id,
    division_id,
    vacation_id
FROM account;